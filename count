#include <bits/stdc++.h>
using namespace std;
int main(){
    char sentence[1000];
    cin.getline(sentence,1000);
    int i=0,count=0;
    while(sentence[i]!='\0'){
        if(isspace(sentence[i])){
            count++;
        }
        i++;
    }
    cout<<"words in sentence:"<<count<<endl;
}
